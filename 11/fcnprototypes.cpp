#include <iostream>
#include <iterator>
#include <utility>
#include <vector>

struct Employee{

};

int sumTo(int value);

void printEmployeeName(const Employee& e);

std::pair<int, int> minmax(int v1, int v2);

std::size_t getIndexOfLargestValue(const std::vector<int>& v);

const std::string& getElement(const std::vector<std::string>& v);

double max(double a, double b);

void swap(int&a, int &b);

int& getLargestElement(int* array, const size_t length);

int main(){


}